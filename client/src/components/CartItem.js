import React from "react";
// link 
import { Link } from "react-router-dom";

const CartItem = ({item}) => {
  return (
    <div className="flex">
      <Link>
      <img src={`http`} alt=""/>
      </Link>
    </div>
  );
};

export default CartItem;
